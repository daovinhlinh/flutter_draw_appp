name: flutter_draw_appp
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  device_info_plus: ^9.1.1
  file_picker: ^6.1.1
  file_saver: ^0.2.9
  flutter:
    sdk: flutter
  flutter_colorpicker: ^1.0.3
  flutter_hooks: ^0.20.4
  flutter_svg: ^2.0.9
  font_awesome_flutter: ^10.6.0
  image_picker: ^1.0.7
  socket_io_client: ^2.0.3+1
  universal_html: ^2.2.4
  url_launcher: ^6.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/svgs/
